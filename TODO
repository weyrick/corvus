- remove boost lib depedency (osx)
- add model passes, declare-use-chain, call graphs, cfg
- investigate config files from phpmd
- keep output generic for use from e.g. ide
- update boost lexer, or switch to lexertl
- investigate other uses of llvm support classes
